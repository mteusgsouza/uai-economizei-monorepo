import { RouteLoading } from "@/components/ui/route-loading";

/** Fallback de qualquer rota do storefront que não tenha esqueleto próprio. */
export default function Loading() {
  return <RouteLoading />;
}
