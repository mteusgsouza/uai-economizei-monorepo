model User {
  id           String   @id @default(uuid())
  kindeId      String?
  firstName    String?
  lastName     String?
  email        String   @unique
  passwordHash String?
  picture      String?
  createdAt    DateTime @default(now())
  posts        Post[]
}
